generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Pin {
  id        String   @id @default(uuid())
  latitude  Float
  longitude Float
  title     String?
  createdAt DateTime @default(now())
}
